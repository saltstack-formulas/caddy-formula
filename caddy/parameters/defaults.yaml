# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: caddy
  rootgroup: root
  config: '/etc/caddy'
  service:
    name: caddy
  subcomponent:
    config: '/etc/caddy-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
